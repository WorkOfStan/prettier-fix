---
name: Prettier-fix
on: [pull_request, push]

permissions:
  contents: write

jobs:
  prettier-fix:
    runs-on: ubuntu-latest
    # Limit the running time
    timeout-minutes: 10
    steps:
      #- name: Checkout
      #  uses: actions/checkout@v4

      - name: Invoke the prettier fix
        uses: WorkOfStan/prettier-fix@WorkOfStan-patch-1

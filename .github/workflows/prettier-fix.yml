---
name: Prettier-fix
on: [pull_request, push]

permissions:
  contents: write

jobs:
  prettier-fix:
    runs-on: ubuntu-latest
    # Limit the running time
    timeout-minutes: 10
    steps:
      - name: Invoke the Prettier fix
        # Use the latest minor and patch updates under the major version v1.
        uses: WorkOfStan/prettier-fix@v1
        #with:
        #  node-version: "20"
